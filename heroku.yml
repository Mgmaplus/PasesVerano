setup:
  config: {}
build:
  packages:
    - gdal-bin
    -libgdal-grass 
    -python-gdal 
    -geotiff-bin 
    -libgeotiff-epsg 
    -libhdf4-doc
    -libhdf4-alt-dev 
    -hdf4-tools 
    -libmyodbc 
    -odbc-postgresql 
    -tdsodbc 
    -unixodbc-bin
    -ogdi-bin
  languages:
    - python
run:
  web: sh setup.sh && streamlit run crunch.py
